React router dom : <Outlet />  is used to specify where to render the children for a root component,

Outlet is replaced by Listing or productdetail page according to the url route

createRoutesFromElements : we can use JSX too to configure routes, using <Routes> and <Route>